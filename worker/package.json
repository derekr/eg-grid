{
  "name": "eg-grid-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "pnpm sync-sources && wrangler deploy",
    "sync-sources": "node --experimental-strip-types scripts/sync-sources.ts"
  },
  "devDependencies": {
    "esbuild-wasm": "^0.24.0",
    "wrangler": "^4.0.0"
  }
}
